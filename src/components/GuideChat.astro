---
// Phase 2 placeholder — the persistent "Ask the guide" chat widget will live here.
// When ready: a floating, dismissible bottom-right widget that talks to a small
// retrieval+chat service backed by article content + Silas. Subtle, not intrusive.
// For now this component renders nothing so the slot is reserved without affecting layout.
---
{/* GuideChat — Phase 2 */}
